#include <iostream>
#include <vector>

#include "lex.hpp"

using namespace laserc;

std::vector<token>* laserc::lex(std::istream &file)
{
    auto result = new std::vector<token>();
    return result;
}
